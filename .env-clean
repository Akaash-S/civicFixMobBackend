# CivicFix Backend - AWS RDS & S3 Configuration Template
# All variables are REQUIRED for both development and production

# Flask Configuration
FLASK_ENV=production
SECRET_KEY=your-super-secret-key-change-this-in-production

# AWS RDS Database Configuration (REQUIRED)
DATABASE_URL=postgresql://username:password@your-rds-endpoint:5432/database_name

# AWS Configuration (REQUIRED)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_S3_BUCKET_NAME=your-s3-bucket-name
AWS_REGION=us-east-1

# Firebase Configuration (REQUIRED for authentication)
# Generate Base64 from your service account JSON:
# python -c "import base64; print(base64.b64encode(open('service-account.json').read().encode()).decode())"
FIREBASE_SERVICE_ACCOUNT_B64=your-base64-encoded-firebase-service-account
FIREBASE_PROJECT_ID=your-firebase-project-id

# CORS Configuration (Optional)
CORS_ORIGINS=*